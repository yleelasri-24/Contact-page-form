**Level:** Beginner → Intermediate

**Concepts Tested:**

Forms, inputs, labels, CSS selectors, box model, display

### 📌 Task

Build a **Contact Us page** with a styled form.

### 📁 Folder Structure

```
contact-form/
├── index.html
└── style.css

```

### 📄 Requirements

### HTML Form Fields

- Name (text input)
- Email (email input)
- Phone number (number or tel input)
- Subject (dropdown using `<select>`)
- Message (textarea)
- Submit button

Each input **must have a label**.

### Layout Rules

- Form should be **centered on the page**
- Each input should appear on a **new line**
- Use `<section>` or `<article>` to wrap the form

### CSS Requirements

- Use:
    - Padding inside inputs
    - Margin between fields
    - Border styling for inputs
- Set form width (example: 400px)
- Style the submit button differently from inputs
- Use `display: block` where needed

### 🚫 Restrictions

- ❌ No form submission logic
- ❌ No JS validation
- ❌ No advanced selectors

### 🎯 Evaluation Criteria

- Proper form structure
- Clean alignment
- Correct label-input association
- Box model usage
